name: Create Pull Request

on:
  pull_request:
    pull_request:
      types: [opened, reopened]

jobs:
  Build_Distribution_Files:
    runs-on: ubuntu-latest
    steps:
      - name: Build Packages
        run: yarn build
