{
  "name": "aws-amplify-hooks",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/applyinnovations/aws-amplify-hooks.git",
  "license": "MIT",
  "scripts": {
    "build:auth": "cd packages/auth && yarn build",
    "build:datastore": "cd packages/datastore && yarn build",
    "build:datastore2": "cd packages/datastore2 && yarn build",
    "build": "yarn build:auth && yarn build:datastore && yarn build:datastore2"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "aws-amplify": "4.2.5",
    "react": "17.0.2"
  },
  "workspaces": [
    "packages/*"
  ],
  "private": true
}
